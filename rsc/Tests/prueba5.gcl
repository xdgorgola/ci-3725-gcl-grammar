|[
  declare
    max_ : int;
    A : array[0..5]

    max_ := A[0];
    for _ in 1 to 5  -->
       if max_ < A[_] -->
          max_ := A[_]
       [] max_ >= A[_] -->
          skip
       fi
    rof
    // Esto es un comentario. Debe ser ignorado.
]| 