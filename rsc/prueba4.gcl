|[
  declare
    i, max : int;
    itIs : bool;
    A : array[0..5]

    itIs := false;
    i := 0
    do i <= 5 /\ !itIs -->
       itIs := A[i] == 10;
       i := i+1
    od
    // Esto es un comentario. Debe ser ignorado.
]| 